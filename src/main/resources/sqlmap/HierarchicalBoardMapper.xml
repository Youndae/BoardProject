<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.board.project.mapper.HierarchicalBoardMapper">
	
	<!-- <select id="countBoard" resultType="int">
		SELECT count(*) FROM pagetest
	</select>
	
	<select id="selectBoard" resultType="HierarchicalBoardVO">
		SELECT *
		FROM (
			SELECT ROWNUM RN, A.*
			FROM (
				SELECT *
				FROM pageTest
				ORDER BY boardNo DESC
				) A
		)
		WHERE RN BETWEEN #{start} AND #{end}
	</select>
	 -->
	 
	 
	 <!-- <select id="SearchPage" resultType="HierarchicalBoardVO">
		SELECT  boardNo, boardUpperNo, boardTitle, boardDate, boardGroupNo, boardIndent
		FROM (
        SELECT  LEVEL AS "LEVEL", boardNo, boardUpperNo, boardTitle, boardDate, boardGroupNo, boardIndent
                ROW_NUMBER () OVER(ORDER BY boardNo DESC) AS RNUM
        FROM    HierarchicalBoard
        WHERE   1=1
        	<include refid="search"></include>
                    )
		WHERE RNUM BETWEEN ${rowStart} AND ${rowEnd}
		START WITH boardUpperNo = 0
		CONNECT BY PRIOR boardNo = boardUpperNo
		ORDER SIBLINGS BY boardGroupNo DESC
	</select> -->
	 
	 
	 
	<!-- <select id="SearchPage" resultType="HierarchicalBoardVO">
		SELECT  boardNo, boardUpperNo, boardTitle, boardDate, boardGroupNo, boardIndent
		FROM (
        SELECT  LEVEL AS "LEVEL", boardNo, boardUpperNo, boardTitle, boardDate, boardGroupNo, boardIndent,
                ROW_NUMBER () OVER(ORDER BY boardNo DESC) AS RNUM
        FROM    HierarchicalBoard
        WHERE   1=1
        	<include refid="search"></include>
                    )
		START WITH boardUpperNo = 0
		WHERE RNUM BETWEEN ${rowStart} AND ${rowEnd}
		CONNECT BY PRIOR boardNo = boardUpperNo
		ORDER SIBLINGS BY boardGroupNo DESC
	</select> -->
	
	<select id="SearchPage" resultType="HierarchicalBoardVO">
		SELECT  *
		FROM (
        SELECT  ROWNUM AS RNUM, A.*
        FROM    (
                select * 
                from test 
                WHERE 1=1
				<include refid="search"></include>
                
                START WITH boardUpperNo = 0
                CONNECT BY PRIOR boardNo = boardUpperNo
                ORDER SIBLINGS BY boardGroupNo DESC
                ) A
                 
                    ) 
		WHERE RNUM BETWEEN ${rowStart} AND ${rowEnd}
        
		
	</select>
	
	<select id="ListCount" resultType="int">
		SELECT 	COUNT(boardNo)
		FROM	test
		WHERE 1=1
			<include refid="search"></include>
		AND boardNo > 0
	</select>
	
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 't'.toString()"> AND boardTitle LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'c'.toString()"> AND boardContent LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'w'.toString()"> AND userId LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'tc'.toString()"> AND (boardTitle LIKE '%' || #{keyword} || '%') or (boardContent LIKE '%' || #{keyword} || '%')</if>
		</if>
	</sql>
	
	
	
	

	<select id="BoardList" resultType="HierarchicalBoardVO">
		SELECT LEVEL AS "LEVEL", boardNo, boardUpperNo, boardTitle, boardDate, boardGroupNo, boardIndent
		FROM HierarchicalBoard
		START WITH boardUpperNo = 0
		CONNECT BY PRIOR boardNo = boardUpperNo
		ORDER SIBLINGS BY boardGroupNo DESC
	</select>

	<select id="BoardDetail" resultType="HierarchicalBoardVO">
		SELECT boardNo, boardTitle, boardContent, userId
		FROM HierarchicalBoard 
		WHERE boardNo=#{boardNo}
	</select>

	<update id="BoardModifyProc" parameterType="HierarchicalBoardVO">
		UPDATE HierarchicalBoard
		SET boardTitle=#{boardTitle}, boardContent=#{boardContent}
		WHERE boardNo=#{boardNo}
	</update>
	
	<delete id="BoardDelete" parameterType="int">
		DELETE FROM HierarchicalBoard WHERE boardNo=#{boardNo}
	</delete>

	<insert id="BoardInsertProc" parameterType="HierarchicalBoardVO">
		INSERT INTO	HierarchicalBoard
		(boardNo, boardTitle, userId, boardContent, boardDate, boardGroupNo, boardUpperNo, boardIndent)
		VALUES(board_seq.NEXTVAL, #{boardTitle}, #{userId}, #{boardContent}, sysdate, board_seq.NEXTVAL, 0, 0)
	</insert>

	<select id="BoardReply" resultType="HierarchicalBoardVO">
		SELECT boardNo, boardTitle, boardGroupNo, boardIndent
		FROM HierarchicalBoard where boardNo=#{boardNo}
	</select>

	<insert id="BoardReplyProc" parameterType="HierarchicalBoardVO">
		insert into
		HierarchicalBoard
		(boardNo, boardTitle, userId, boardContent,
		boardDate, boardGroupNo, boardUpperNo, boardIndent)
		values(board_seq.NEXTVAL, #{boardTitle}, #{userId}, #{boardContent},
		sysdate, #{boardGroupNo}, #{boardNo}, #{boardIndent})
	</insert>

</mapper> 