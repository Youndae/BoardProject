<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.project.mapper.ImageBoardMapper">


	<select id="imageList" resultType="ImageDataVO">
		SELECT a.ImageNo, a.imageTitle, a.UserId, b.ImageData
		FROM (
        	SELECT ImageNo, ImageData
       		FROM ImageData 
       		WHERE ImageNo > 0 
       		AND rowid in(select max(rowid) from ImageData group by ImageNo)
    		)b INNER JOIN ImageBoard a
		ON a.ImageNo = b.ImageNo
		ORDER BY ImageNo DESC
	</select>
	
	<select id="imageDetail" resultType="ImageDataVO">
		SELECT a.ImageNo, a.ImageTitle, a.ImageContent, a.UserId, b.ImageData
		FROM ImageBoard a INNER JOIN ImageData b
		ON a.ImageNo = b.ImageNo
		WHERE a.ImageNo=#{ImageNo}
		ORDER BY b.ImageStep ASC
	</select>
	
	<select id="imageModify" resultType="ImageBoardVO">
		select * from imageBoard where ImageNo=#{ImageNo}
	</select><!-- 여기도 계층형과 마찬가지로 detail 체크. -->
	
	<!-- <insert id="imageInsertProc" parameterType="String">
		insert into imageBoard
		(imageNo, imageTitle, imageWriter, image1, image2, image3, image4, image5, imageDate)
		values(image_seq.NEXTVAL, #{imageTitle}, #{imageWriter}, #{image1}, #{image2}, #{image3}, #{image4}, #{image5}, sysdate)
	</insert> -->
	
	<insert id="imageInsertProc" parameterType="ImageBoardVO">
		INSERT INTO ImageBoard(
			ImageNo
			, ImageTitle
			, UserId
			, ImageContent
			, ImageDate)
		VALUES (
			Image_SEQ.NEXTVAL
			, #{ImageTitle}
			, #{UserId}
			, #{ImageContent}
			, sysdate
			)
	</insert>
	
	
	<select id="ModifyTest" resultType="ImageBoardVO">
		SELECT ImageTitle, UserId, ImageNo, ImageContent
		FROM ImageBoard
		WHERE ImageNo=#{ImageNo}
	</select>
	
	<select id="getAttachList" resultType="ImageDataVO">
		SELECT ImageData, OldName, ImageStep
		FROM ImageData
		WHERE ImageNo=#{ImageNo} ORDER BY ImageStep ASC
	</select>
	
	
	<insert id="imageInsert" parameterType="ImageDataVO">
		INSERT INTO ImageData(
			ImageData
			, ImageNo
			, OldName
			, ImageStep)
		VALUES(
			#{ImageData}
			, Image_SEQ.CURRVAL
			, #{OldName}
			, #{ImageStep}
		)
	</insert>
	
	<delete id="deletefiles" parameterType="String">
		DELETE FROM ImageData WHERE ImageData=#{ImageData}
	</delete>
	
	<update id="ModifyProc" parameterType="ImageBoardVO">
		UPDATE ImageBoard SET ImageTitle=#{ImageTitle}, ImageContent=#{ImageContent} WHERE ImageNo=#{ImageNo}
	</update>
	
	<select id="CountStep" resultType="int">
		select MAX(ImageStep) from ImageData where imageNo=#{imageNo}
	</select>
	
	<insert id="imageModfiy" parameterType="ImageDataVO">
		INSERT INTO ImageData(
			ImageData
			, ImageNo
			, OldName
			, ImageStep)
		VALUES(
			#{ImageData}
			, #{ImageNo}
			, #{OldName}
			, #{ImageStep}
		)
	</insert>
	
	<delete id="imageDelete" parameterType="int">
		DELETE FROM ImageBoard WHERE ImageNo=#{ImageNo}
	</delete>
	
	<delete id="imageDataDelete" parameterType="int">
		DELETE FROM ImageData WHERE ImageNo=#{ImageNo}
	</delete>

</mapper> 